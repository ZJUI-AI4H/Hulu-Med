# PyTorch built against CUDA 11.8 wheels (matches the README install step)
--extra-index-url https://download.pytorch.org/whl/cu118

# ---- Core DL stack (kept consistent with the manual install step) ----
torch==2.4.0
torchvision==0.19.0
transformers==4.51.2
tokenizers>=0.21,<0.22          # transformers 4.51 requires tokenizers 0.21.x
huggingface_hub>=0.30.0,<1.0    # transformers 4.51 requires hub >= 0.30
accelerate==1.7.0
datasets==2.21.0

# ---- Training / fine-tuning ----
deepspeed==0.15.4
peft==0.4.0
bitsandbytes==0.43.3            # prebuilt wheel targets CUDA 12.x;
                                # on CUDA 11.8 you may need to build from source

# ---- Medical & vision modeling ----
nibabel==5.3.2
timm==1.0.3
numpy==1.24.4

# ---- Data / video processing ----
decord==0.6.0
imageio==2.34.0
imageio-ffmpeg==0.4.9
moviepy==1.0.3
opencv-python==4.6.0.66
pyarrow
pysubs2
ffmpeg-python

# ---- Misc utilities ----
scikit-learn==1.2.2
sentencepiece==0.1.99
shortuuid
einops==0.6.1
einops-exts==0.0.4
pydantic>=2.0
markdown2[all]
gradio==3.50.0
gradio_client==0.6.1
httpx==0.24.1
requests
openai
uvicorn
fastapi
tensorboard
wandb
swanlab
tabulate
Levenshtein
